services:
  monitor:
    build: ./monitor
    ports:
      - "8080:8080"
    environment:
      MONITOR_TOKEN: "${MONITOR_TOKEN:-change-me}"
    volumes:
      - monitor-data:/data
    restart: unless-stopped

volumes:
  monitor-data:
